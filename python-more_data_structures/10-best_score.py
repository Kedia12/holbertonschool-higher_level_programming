#!/usr/bin/python3
def best_score(a_dictionary):
    biggest_integer = a_dictionary[key]
    for key in dictionary:
        if key > biggest_integer:
            biggest_integer = key
    return biggest_integer
